<!DOCTYPE html>
<html>
  <head>
    <!--script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.0/jquery.min.js"></script-->
    <script src="/static/jquery.min.js"></script>
    <% if (!user) { %>
    <script src="https://login.persona.org/include.js" type="text/javascript"></script>
    <% } %>
    <script language="javascript">
      $(document).ajaxStop(function() {
        $('#syncing').css('visibility','hidden');
      });
      $(document).ajaxStart(function() {
        $('#syncing').css('visibility','visible');
      });
    </script>
    <link rel="stylesheet" href="/static/style.css">
    <title>Moral market</title>
  </head>
  <body>
    <p>
    <a href="/">Main page</a> |
    <% if (!user) { %>
      <% include login_button %>
    <% } else { %>
      <%= user.email %> | <%= user.points %> points |
      <a href="/logout">Logout</a>
    <% } %>
    <span id="syncing" style="visibility:hidden">-- Syncing --</span>
    </p>

<%
var user_picker_name;
var user_picker_rowid;
var project_picker_name;
var project_picker_rowid;
%>
